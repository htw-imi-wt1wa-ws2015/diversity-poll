<%- model_class = Value -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.titleize %></h1>
</div>

<dl class="dl-horizontal">
  <dt><strong><%= model_class.human_attribute_name(:label) %>:</strong></dt>
  <dd><%= @value.label %></dd>
  <dt><strong><%= model_class.human_attribute_name(:dimension_id) %>:</strong></dt>
  <dd><%= @value.dimension_id %></dd>
</dl>

<%= link_to t('.back', :default => t("helpers.links.back")),
              values_path, :class => 'btn btn-default'  %>
<%= link_to t('.edit', :default => t("helpers.links.edit")),
              edit_value_path(@value), :class => 'btn btn-default' %>
<%= link_to t('.destroy', :default => t("helpers.links.destroy")),
              value_path(@value),
              :method => 'delete',
              :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
              :class => 'btn btn-danger' %>
